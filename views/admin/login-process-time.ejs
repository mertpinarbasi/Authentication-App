<!DOCTYPE html>
<html>

<head>

    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">


    <title>Login Process Time </title>

    <link rel="stylesheet" type="text/css"
        href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css">

    <style type="text/css">
        body {
            background-color: #DADADA;
        }

        body>.grid {
            height: 100%;
        }

        .image {
            margin-top: -100px;
        }

        .column {
            max-width: 450px;
        }
    </style>

</head>

<body>

    <div class="ui large aligned center aligned grid">
        <div class="column">
            <h2 class="ui violet image header">
                <div class="content">
                    Login Process Time
                </div>
            </h2>

            <div class="ui message center aligned ">
                <h3>Total number of successful login attempts : <%= `${locals.loginInformation.average} ms ` %>
                </h3>

                <h3>The average login time of latest day :
                    <%= `${locals.loginInformation.logNumber} ` %>
                </h3>

                <%locals.loginInformation.logs.forEach((log)=>{ %>
                    <div class="ui  centered  wide card">

                        <div class="content">
                            <a class="header">
                                <%= log.loginUser %>

                            </a>
                            <%=`${parseFloat(log.loginTime.toFixed(5)) } ms` %>
                                <div class="meta">
                                    <span class="date">
                                        <%= log.loginDate%>
                                    </span>
                                </div>%>

                        </div>
                    </div>
                    <% }) %>


            </div>


        </div>


    </div>


</body>

</html>